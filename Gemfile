# frozen_string_literal: true

source 'https://rubygems.org'

ruby '2.5.1'

# exifr gem used for reading the EXIF data from images.
gem 'exifr'

group :development do
  # rubocop gem used foe keeping good ruby code standards
  gem 'rubocop'
end

group :test do
  # rspec gem used for better testing
  gem 'rspec', '~> 3.0'
end
